# =============================================================================
# MCP-Linx — пример переменных окружения (C4)
#
# Использование:
#   cp .env.example .env
#   `.env` в .gitignore; `.env.example` — в репозитории (см. .gitignore:27-29).
#
# `Settings` (src/mcp_linx/main.py:36, pydantic-settings) читает файл `.env`
# из текущего каталога. ВАЖНО (A10): env_prefix у `Settings` НЕТ, поэтому
# имена вида LINX_* НЕ маппятся на поля — используйте имена ниже.
# Регистр имён не важен (pydantic-settings сопоставляет case-insensitive).
# =============================================================================

# --- Settings: поля, которые реально читаются кодом (5) ----------------------

# Имя сервера, объявляется MCP-клиентам (main.py:123)
MCP_SERVER_NAME=mcp-linx

# Версия сервера (main.py:124)
MCP_SERVER_VERSION=1.0.0

# Путь к конфигу; переопределяет дефолт `config/settings.yaml` (main.py:109)
CONFIG_PATH=config/settings.yaml

# Уровень логирования: DEBUG | INFO | WARNING | ERROR (main.py:49)
LOG_LEVEL=INFO

# Harness-цикл: default | streaming (main.py:128)
AGENT_LOOP=default

# `PLUGINS` НЕ поддерживается (A10, FIXED 2026-09-17): поле `Settings.plugins`
# удалено как мёртвое — состав плагинов задаёт только
# `config/settings.yaml::plugins.enabled` (PluginManager.load_plugins()).

# --- Секреты: только окружение запускающего процесса, НЕ этот .env ----------
# Не раскомментируйте секреты ниже в .env: Settings отвергает неизвестные поля
# (extra_forbidden). Подстановка YAML читает os.environ; dotenv Settings туда
# значения не экспортирует. Передавайте секреты через окружение/secret manager.
# Поддерживается ${VAR:-default}; пустое значение не включает default.
# Нет переменной без default → warning + ВЕСЬ исходный YAML без подстановки.
# Подстановка до YAML-разбора, без escaping: учитывайте кавычки в значениях.
# Примеры запуска: docs/DEVELOPMENT.md#environment-and-configuration.
#
# Имена окружения и соответствующие ключи YAML (не dotenv-присваивания):
# POSTGRES_PASSWORD → plugins.postgres.password
# REDIS_PASSWORD    → plugins.redis.password
# PROMETHEUS_TOKEN  → plugins.prometheus.token
# LOKI_TOKEN        → plugins.loki.token

# --- docker-compose.yml (только для Docker-профиля) --------------------------

# Каталог с SSH-ключами к целевым хостам; монтируется в /home/mcp/.ssh:ro
# (интерполяция docker compose, НЕ читается Python-кодом). Дефолт: ~/.ssh
# Задавайте SSH_KEY_DIR в окружении shell, НЕ в серверном .env (extra_forbidden).
# Например, перед запуском compose: export SSH_KEY_DIR="$HOME/.ssh"