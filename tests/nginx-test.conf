# Nginx config for integration tests — stub_status endpoint
server {
    listen 80;
    server_name _;

    location / {
        return 200 "integration-ok\n";
        add_header Content-Type text/plain;
    }

    location /nginx_status {
        stub_status on;
        access_log off;
    }
}